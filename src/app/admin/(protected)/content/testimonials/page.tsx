import Link from "next/link";
import { AdminPageHeader, EmptyState, StatusBadge, formatDate } from "@/components/admin/ui";
import { getAdminTestimonials } from "@/lib/admin/data";

export const metadata={title:"Testimonials"};
export default async function TestimonialsPage(){const rows=await getAdminTestimonials();return <main className="admin-page"><AdminPageHeader eyebrow="Content / Testimonials" title="Testimonials" description="Manage verified client perspectives and homepage placement."><Link className="admin-button admin-button--primary" href="/admin/content/testimonials/new">New testimonial</Link></AdminPageHeader><section className="admin-panel">{rows.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Client</th><th>Company</th><th>Placement</th><th>Status</th><th>Updated</th></tr></thead><tbody>{rows.map((row)=><tr key={row.id}><td><strong><Link href={`/admin/content/testimonials/${row.id}`}>{row.client_name}</Link></strong><small>{row.job_title||"Client"}</small></td><td>{row.company||"—"}</td><td>{row.featured?`Featured · ${row.sort_order}`:"Standard"}</td><td><StatusBadge status={row.published?"published":"draft"}/></td><td>{formatDate(row.updated_at)}</td></tr>)}</tbody></table></div>:<EmptyState title="No testimonials yet" copy="Add the first approved client quote. If you just deployed this feature, run migration 003 first." href="/admin/content/testimonials/new" action="Add testimonial"/>}</section></main>}
