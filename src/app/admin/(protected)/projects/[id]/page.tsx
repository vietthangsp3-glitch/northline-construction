import { notFound } from "next/navigation"; import { AdminPageHeader } from "@/components/admin/ui"; import { ProjectEditor } from "@/components/admin/project-editor"; import { getAdminProject } from "@/lib/admin/data";
export default async function EditProjectPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string}>}){const {id}=await params;const project=await getAdminProject(id);if(!project)notFound();const saved=(await searchParams).saved;return <main className="admin-page"><AdminPageHeader eyebrow="Projects / Editor" title={project.title} description="Edit content, media, SEO, and publishing status."/>{saved&&<div className="admin-alert" role="status">Project changes saved successfully.</div>}<ProjectEditor project={project}/></main>}
