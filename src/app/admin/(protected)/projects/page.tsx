import Link from "next/link";
import { importStarterContent } from "@/app/admin/(protected)/actions";
import { AdminPageHeader, EmptyState, StatusBadge, formatDate } from "@/components/admin/ui";
import { getAdminProjects } from "@/lib/admin/data";

export const metadata = { title: "Projects" };
export default async function AdminProjectsPage() { const projects = await getAdminProjects(); return <main className="admin-page"><AdminPageHeader eyebrow="Content / Portfolio" title="Projects" description="Create, edit, publish, and curate the Northline portfolio."><form action={importStarterContent}><button className="admin-button" type="submit">Import current portfolio</button></form><Link className="admin-button admin-button--primary" href="/admin/projects/new">New project ＋</Link></AdminPageHeader><section className="admin-panel">{projects.length ? <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Project</th><th>Category</th><th>Location</th><th>Status</th><th>Featured</th><th>Updated</th><th/></tr></thead><tbody>{projects.map((project) => <tr key={project.id}><td><strong><Link href={`/admin/projects/${project.id}`}>{project.title}</Link></strong><small>/{project.slug}</small></td><td>{project.category}</td><td>{project.location || "—"}</td><td><StatusBadge status={project.status}/></td><td>{project.featured ? "Yes" : "—"}</td><td>{formatDate(project.updated_at)}</td><td><Link href={`/admin/projects/${project.id}`}>Edit ↗</Link></td></tr>)}</tbody></table></div> : <EmptyState title="No managed projects" copy="Import the current public portfolio, or create a project from scratch." href="/admin/projects/new" action="Create project"/>}</section></main>; }
