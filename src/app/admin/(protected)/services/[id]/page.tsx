import { notFound } from "next/navigation";import { AdminPageHeader } from "@/components/admin/ui";import { ServiceEditor } from "@/components/admin/service-editor";import { getAdminService,getMediaAssets } from "@/lib/admin/data";
export default async function EditServicePage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string}>}){const {id}=await params;const [service,assets]=await Promise.all([getAdminService(id),getMediaAssets()]);if(!service)notFound();const saved=(await searchParams).saved;return <main className="admin-page"><AdminPageHeader eyebrow="Services / Editor" title={service.title} description="Edit service content, related work, and visibility."/>{saved&&<div className="admin-alert" role="status">Service changes saved successfully.</div>}<ServiceEditor service={service} assets={assets}/></main>}
