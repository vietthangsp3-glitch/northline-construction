import Link from "next/link"; import { importStarterContent } from "@/app/admin/(protected)/actions"; import { AdminPageHeader, EmptyState, formatDate } from "@/components/admin/ui"; import { getAdminServices } from "@/lib/admin/data";
export const metadata={title:"Services"}; export default async function AdminServicesPage(){const services=await getAdminServices();return <main className="admin-page"><AdminPageHeader eyebrow="Content / Expertise" title="Services" description="Manage offerings, order, visibility, and service page content."><form action={importStarterContent}><button className="admin-button" type="submit">Import current services</button></form><Link className="admin-button admin-button--primary" href="/admin/services/new">New service ＋</Link></AdminPageHeader><section className="admin-panel">{services.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Order</th><th>Service</th><th>Active</th><th>Featured</th><th>Updated</th><th/></tr></thead><tbody>{services.map((service)=><tr key={service.id}><td>{service.number}</td><td><strong><Link href={`/admin/services/${service.id}`}>{service.title}</Link></strong><small>/{service.slug}</small></td><td><span className={`admin-badge admin-badge--${service.active?"published":"archived"}`}>{service.active?"Active":"Disabled"}</span></td><td>{service.featured?"Yes":"—"}</td><td>{formatDate(service.updated_at)}</td><td><Link href={`/admin/services/${service.id}`}>Edit ↗</Link></td></tr>)}</tbody></table></div>:<EmptyState title="No managed services" copy="Import the current public services or create a new offering." href="/admin/services/new" action="Create service"/>}</section></main>}
